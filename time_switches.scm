(declare (unit time_switches))

;;; handle time input
(define time_switch_handler
  (lambda (arguments)
    (display "oops, not implemented yet!")
    (newline)
    #t))
    ;(for-each
    ; (lambda (item)
    ;   (display item)
    ;   (newline))
    ; (arguments))
    ;(exit)))

